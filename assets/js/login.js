// ConceptOS™ Login Logic (Future Ready)
// Role-based redirection will be handled server-side later

document.querySelector(".login-form").addEventListener("submit", e => {
  e.preventDefault();
  alert("Login system will be activated with backend integration.");
});
